
it('should calculate the monthly rate correctly', function (calculateMonthlyPayment) {
  let obj = {
    amount: 1000,
    years: 8,
    rate: 5.8
  }
  expect(calculateMonthlyPayment(obj)).toEqual()
});


it("should return a result with 2 decimal places", function() {
  // ..
});

/// etc
